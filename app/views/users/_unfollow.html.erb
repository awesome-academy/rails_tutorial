<%= button_to "Unfollow", relationship_path(current_user.active_relationships.find_by(followed_id: user.id)),
              method: :delete,
              data: { turbo_stream: true },
              class: "btn btn-secondary" %>
